{
  "private": true,
  "name": "focal",
  "scripts": {
    "build": "yarn workspace focal-sans-react build",
    "package": "yarn workspace focal-sans-react pack",
    "release": "yarn workspace focal-sans-react release",
    "test": "yarn workspace focal-sans-react test",
    "postinstall": "yarn build"
  },
  "devDependencies": {
    "@grammarly/tslint-config": "0.5.1",
    "tslint": "5.2.0",
    "typescript": "3.3.3"
  },
  "workspaces": [
    "packages/focal"
  ]
}
